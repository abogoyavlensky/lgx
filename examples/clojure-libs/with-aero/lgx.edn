{:main "main.lg"
 :targets {:bin {:out "bin/with-lib"}}
 ;; aero has no runtime deps beyond clojure.edn / clojure.java.io (both let-go
 ;; builtins), so only aero itself is listed.
 :deps {juxt/aero {:git/url "https://github.com/juxt/aero"
                   :git/tag "1.1.6"}}}
