(ns main)

(defn- main []
  (println "hello from lgx-managed let-go"))

; Top-level forms run during AOT compilation too. Guard so `lg -b` doesn't
; execute the CLI while bundling.
(when-not *compiling-aot*
  (main))
